# Apollo Server Federation Starter

## Overview

This repository contains a basic setup of Apollo Server with TypeScript that is ready
for schema federation. The server currently has lowdb configured in order to get up
and running quickly.